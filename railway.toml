[build]
builder = "nixpacks"

[deploy]
startCommand = "playwright install chromium && playwright install-deps chromium && gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
