[build]
builder = "nixpacks"

[deploy]
# Gunakan perintah ini HANYA jika file app.py sudah benar
startCommand = "python -m playwright install --with-deps chromium && gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3